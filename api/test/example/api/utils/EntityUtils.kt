package example.api.utils

// fun createUser(dataSource: DataSource, user: User) = dataSource.connection.use { connection ->
//    CreateUserQuery().query(
//        connection,
//        CreateUserParams(user.firstName, user.lastName, user.lastName, user.email)
//    ).first()
// }
